<div class="container">
    <h1>WELCOME TO CanITakeIt <%= name %></h1>
    <ul>
        <li> waiting
        <% for(var i = 0; i <data.length; i++){%>
            <% for(var y = 0; y <dataP.length; y++){%>
                <% if(data[i].status == 'waiting' && data[i].plats_id == dataP[y].id ){ %>
                    <ul>
                        <a>name: <%= dataP[y].name %> reference: <%= data[i].id %></a>
                        <a href="/promoteStatus/<%=data[i].id%>">PROMOTE</a>
                    </ul>
                <%}%>  
            <%}%> 
        <%}%>
        </li>
        <li> preparation
            <% for(var i = 0; i <data.length; i++){%>
                <% for(var y = 0; y <dataP.length; y++){%>
                    <% if(data[i].status == 'preparation' && data[i].plats_id == dataP[y].id ){ %>
                        <ul>
                            <a>name: <%= dataP[y].name %> reference: <%= data[i].id %> </a>
                            <a href="/promoteStatus/<%=data[i].id%>">PROMOTE</a>
                        </ul>
                    <%}%>  
                <%}%> 
            <%}%>
        </li>
        <li> ready
            <% for(var i = 0; i <data.length; i++){%>
                <% for(var y = 0; y <dataP.length; y++){%>
                    <% if(data[i].status == 'ready' && data[i].plats_id == dataP[y].id ){ %>
                        <ul>
                            <a>name: <%= dataP[y].name %>  reference: <%= data[i].id %></a>
                        </ul>
                    <%}%>  
                <%}%> 
            <%}%>
        </li>  
    </ul>
</div>